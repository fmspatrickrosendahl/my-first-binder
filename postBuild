set -ex

# downloads a compatible chrome version
pyppeteer-install

# enables /proxy/<port>
jupyter serverextension enable --sys-prefix jupyter_server_proxy

# setup git
git config --global user.email "patrick.rosendahl@fms-logistics.com"
